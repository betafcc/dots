#!/usr/bin/env bash

# Shouldn't source .bashrc
# in Ubuntu it contains a guard
# to check if its been run in interactive
# preventing it from being sourced programatically
source "$HOME/.betafcc/bashrc"
"$@"
